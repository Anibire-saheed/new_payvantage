import USSDPage from "./USSDPage";
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "USSD Platform | Payvantage",
};

export default function Page() {
  return <USSDPage />;
}
