import GamificationPage from "./GamificationPage";
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Gamification | Payvantage",
};

export default function Page() {
  return <GamificationPage />;
}
