import VASPage from "./VASPage";
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Value Added Services | Payvantage",
};

export default function Page() {
  return <VASPage />;
}
